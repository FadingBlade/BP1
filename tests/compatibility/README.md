# Compatibility Test

Release version: 1.0.0

- `source/` is the ordinary unencrypted test website.
- `package/` is the extracted encrypted package ready to upload to a public GitHub repository.
- `BP1-Compatibility-Test-READY.zip` contains `index.bp1` and `bp/` at its root.

Test key: `BP1-TEST-2026`.
